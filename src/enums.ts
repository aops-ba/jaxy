export enum Infix {
  Plus = '+',
  Minus = '-',
  Times = '*',
  Divide = '/',
  Quotient = '#',
  Mod = '%',
  Caret = '^',
  Timestimes = '**',
}

export enum Kind {
  Text,
  Value,
  Apply,
  Tuple,
  Comment,
}